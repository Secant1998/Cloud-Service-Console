# UI Agent 长期需求规范

此文件保留旧命名，作为兼容入口。

当前这套小程序的 UI 规范真身已经迁移到：

- `frontend/UI_AGENT_SPEC.md`

后续 agent 请优先阅读：

1. `frontend/README.md`
2. `frontend/IMPLEMENTATION_LOG.md`
3. `frontend/UI_AGENT_SPEC.md`

如果这里和 `UI_AGENT_SPEC.md` 有任何冲突，以 `UI_AGENT_SPEC.md` 为准。
